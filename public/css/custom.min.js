$(document).ready(function ($) {
    // Go to URL
    $('select.gotourl').on('change', function () {
        var url = $(this).val();
        if (url) { window.location = url; }
        return false;
    });
});